From f025e0265d2a318cb27b6a768390c7bc66f0f92a Mon Sep 17 00:00:00 2001
From: Pitchaiah Prakash <prakash56755@gmail.com>
Date: Sun, 4 Feb 2024 20:32:46 +0100
Subject: [PATCH] Enable Simple var platform driver

---
 drivers/Kconfig                |  3 ++
 drivers/Makefile               |  2 +
 drivers/variscite/Kconfig      | 16 +++++++
 drivers/variscite/Makefile     |  2 +
 drivers/variscite/var_driver.c | 82 ++++++++++++++++++++++++++++++++++
 5 files changed, 105 insertions(+)
 create mode 100644 drivers/variscite/Kconfig
 create mode 100644 drivers/variscite/Makefile
 create mode 100644 drivers/variscite/var_driver.c

diff --git a/drivers/Kconfig b/drivers/Kconfig
index 0d399ddaa185..5338f29dc099 100644
--- a/drivers/Kconfig
+++ b/drivers/Kconfig
@@ -236,4 +236,7 @@ source "drivers/interconnect/Kconfig"
 source "drivers/counter/Kconfig"
 
 source "drivers/most/Kconfig"
+
+source "drivers/variscite/Kconfig"
+
 endmenu
diff --git a/drivers/Makefile b/drivers/Makefile
index a110338c860c..961d943234fa 100644
--- a/drivers/Makefile
+++ b/drivers/Makefile
@@ -187,3 +187,5 @@ obj-$(CONFIG_GNSS)		+= gnss/
 obj-$(CONFIG_INTERCONNECT)	+= interconnect/
 obj-$(CONFIG_COUNTER)		+= counter/
 obj-$(CONFIG_MOST)		+= most/
+obj-$(CONFIG_VARISCITE)		+= variscite/
+
diff --git a/drivers/variscite/Kconfig b/drivers/variscite/Kconfig
new file mode 100644
index 000000000000..50c6fba0210e
--- /dev/null
+++ b/drivers/variscite/Kconfig
@@ -0,0 +1,16 @@
+menuconfig VARISCITE
+	tristate "Variscite Drivers"
+	help
+	  This enables the external peripherals drivers for
+	  variscite modules.
+
+	  If unsure, choose N.
+
+if VARISCITE
+
+config VAR_PLAT_DRIVER
+	tristate "Simple platform driver"
+	help
+	  Say Y here to enable the simple var platform driver.
+
+endif
diff --git a/drivers/variscite/Makefile b/drivers/variscite/Makefile
new file mode 100644
index 000000000000..12db8ac23305
--- /dev/null
+++ b/drivers/variscite/Makefile
@@ -0,0 +1,2 @@
+obj-$(CONFIG_VAR_PLAT_DRIVER)		+= var_driver.o
+
diff --git a/drivers/variscite/var_driver.c b/drivers/variscite/var_driver.c
new file mode 100644
index 000000000000..b294f57165fe
--- /dev/null
+++ b/drivers/variscite/var_driver.c
@@ -0,0 +1,82 @@
+/*
+ * Simple Platform var int driver
+ *
+ * Copyright (c) Prakash <prakash56755@gmail.com>
+ */
+
+#include <linux/err.h>
+#include <linux/device.h>
+#include <linux/kernel.h>
+#include <linux/of.h>
+#include <linux/of_device.h>
+#include <linux/module.h>
+#include <linux/platform_device.h>
+
+#define DRIVER_NAME	"var_int"
+#define MAX_CHAR     20
+#define VAR_DRIVER_VER  "v1.2"
+
+
+struct vardev {
+    struct device *dev;
+    char device_name[20];
+    int var_gpio;
+};
+
+static int var_int_probe(struct platform_device *pdev)
+{
+    pr_info("#### %s() ####\n", __func__);
+    struct device *dev = &pdev->dev;
+    struct device_node *node = dev->of_node;
+    struct vardev * var_dev;
+    int ret_val;
+    const char *var_string;
+
+    var_dev = devm_kzalloc(dev, sizeof(*var_dev), GFP_KERNEL);
+	if (!var_dev)
+		return -ENOMEM;
+
+    var_dev->dev = &pdev->dev;
+
+    dev_info(var_dev->dev, "Driver version: %s\n", VAR_DRIVER_VER);
+
+    ret_val = of_property_read_string(node, "interview-name", &var_string);
+	if (ret_val < 0) {
+        dev_err(var_dev->dev, "Interview name string is missing in platform device-tree.\n");
+        return -EINVAL;
+    }
+
+    dev_info(var_dev->dev, "interview-name string from device tree: %s\n", var_string);
+
+    platform_set_drvdata(pdev, var_dev);
+
+    return 0;
+}
+
+static int var_int_remove(struct platform_device *pdev)
+{
+
+    dev_info(&pdev->dev, "Var_Driver removed: \n");
+    return 0;
+}
+
+static const struct of_device_id var_int_dt_ids[] = {
+	{ .compatible = "interview,challenge", },
+	{ }
+};
+MODULE_DEVICE_TABLE(of, var_int_dt_ids);
+
+static struct platform_driver var_int_driver = {
+	.driver = {
+		.name	= DRIVER_NAME,
+		.of_match_table = var_int_dt_ids,
+	},
+	.probe  = var_int_probe,
+    .remove = var_int_remove,
+};
+
+module_platform_driver(var_int_driver);
+
+MODULE_AUTHOR("Pitchaiah Prakash <prakash56755@gmail.com>");
+MODULE_DESCRIPTION("Simple platform var driver");
+MODULE_LICENSE("GPL v2");
-- 
2.34.1

